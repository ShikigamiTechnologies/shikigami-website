{
  "scripts": {
    "check": "node scripts/check-site.mjs",
    "build": "wrangler deploy --dry-run --env staging --outdir .wrangler-dry-run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  },
  "overrides": {
    "sharp": "0.35.0"
  }
}
